name: Website Testing

on:
  workflow_dispatch:

jobs:
  lighthouse_test:
    runs-on: ubuntu-latest
    steps:
      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v11
        with:
          urls: https://frontend-jmyb.onrender.com/
